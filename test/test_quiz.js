import assert from 'assert';
import quiz from '../src/state/actions/quiz';

describe('quiz', function () {
  describe('#quiz()', function () {
    it('should generate quiz', function (done) {
      done();
    });
  });
});
